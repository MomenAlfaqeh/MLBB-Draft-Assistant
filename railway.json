{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd /app && python -c 'import os; print(os.listdir(\".\")); print(os.getcwd())'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
